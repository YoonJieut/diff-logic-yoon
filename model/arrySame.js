/**
 *  문장 하나를 받아서 
 * 온점과 쉼점을 제거하고 
 * 소문자로 반환하는 함수를 제작
 * 
 * @param {String} input
 * @return {Array} 
 */


export const arrySame = (input)=>{
  // 배열을 나누기, 공백제거
  const inputArry = input.split(/\s+/)
  // console.log("inputArry =",inputArry )
  // map을 통해 온점, 쉼포, 소문자로 바꾸기
  // const result = inputArry.map((value)=>{value.replace(/[.,\.$]/g,'').toLowerCase()});
  
}
